{
  "name": "dhh-fm",
  "version": "0.1.0",
  "private": true,
  "description": "DHH FM — always broadcasting, from the Omarchy bar.",
  "scripts": {
    "test": "node --test test/model.test.js && python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "license": "MIT"
}
